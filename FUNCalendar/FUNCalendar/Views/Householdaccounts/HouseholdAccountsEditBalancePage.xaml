<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:prism="clr-namespace:Prism.Mvvm;assembly=Prism.Forms"
             prism:ViewModelLocator.AutowireViewModel="True"
             x:Class="FUNCalendar.Views.HouseholdAccountsEditBalancePage"
             Title="{Binding BalanceTitle}">
    <StackLayout>
        <Entry Text="{Binding Price.Value}" FontSize="Large" Placeholder="金額を入力"/>
        <Button Text="登録" BackgroundColor="{Binding ErrorColor.Value}" IsEnabled="{Binding CanRegister.Value}" Command="{Binding RegisterHouseholdAccountsBalanceCommand}" />
        <Button Text="キャンセル" BackgroundColor="DarkGray" Command="{Binding CancelCommand}"  />

    </StackLayout>

</ContentPage>